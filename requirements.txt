# Requirements for Task-Arithmetic-for-ZS-IR reproduction
# Tested with Python 3.11, CUDA 12.x
# 
# IMPORTANT: Install in this order to avoid dependency conflicts

# === Core ML ===
torch>=2.0.0
transformers>=4.30.0,<4.46.0    # needs T5 with `past` param in prepare_inputs_for_generation

# === BEIR (Information Retrieval benchmark) ===
# Must use PyPI version (0.x) — NOT GitHub master which broke BM25Search
beir==0.0.9

# === Elasticsearch Python client (must match ES 7.17.x on server) ===
elasticsearch>=7.0.0,<8.0.0

# === Dataset loading ===
datasets>=2.14.0,<3.0.0         # needs loading script support for MIRACL

# === Ranking/Evaluation ===
ranx>=0.3.0

# === Sentence embeddings (needed for MiniLM/RoBERTa experiments in Table 1) ===
sentence-transformers>=2.2.0

# === CLI and utilities ===
click>=8.0.0
tqdm>=4.60.0
